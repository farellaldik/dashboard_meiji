@{
    ViewBag.ContainerClass = "container-fluid";
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">

    <!-- <title>Dashboard - NiceAdmin Bootstrap Template</title> -->
    <meta content="" name="description">
    <meta content="" name="keywords">

    <!-- Google Fonts -->
    <link href="https://fonts.gstatic.com" rel="preconnect">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Nunito:300,300i,400,400i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">

    <!-- Vendor CSS Files -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/assets/vendor/bootstrap-icons/bootstrap-icons.css" rel="stylesheet">
    <link href="~/assets/vendor/boxicons/css/boxicons.min.css" rel="stylesheet">
    <link href="~/assets/vendor/quill/quill.snow.css" rel="stylesheet">
    <link href="~/assets/vendor/quill/quill.bubble.css" rel="stylesheet">
    <link href="~/assets/vendor/remixicon/remixicon.css" rel="stylesheet">
    <link href="~/assets/vendor/simple-datatables/style.css" rel="stylesheet">

    <!-- Template Main CSS File -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

    <!--Swal Alert-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <link href="~/assets/css/style.css" rel="stylesheet">

</head>

<body>

    <main id="main" class="main">

        <div class="pagetitle">
            <h1>Dashboard</h1>
            <nav>
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active">Dashboard</li>
            </nav>
        </div><!-- End Page Title -->

        <section class="section dashboard">

            <div class="row gx-2">
                <div class="col-lg-4 d-flex flex-column align-items-stretch">
                    <div class="card info-card" style="height: 100%">

                        <div class="d-flex flex-row">
                            <!-- Bagian Kiri: Gambar Profil -->
                            <div class="d-flex align-items-center p-0 pt-3 pb-3 pl-3">
                                <img src="~/assets/img/anya.jpeg" alt="Profile Picture" class="profile-img">
                            </div>

                            <!-- Bagian Kanan: Konten Card -->
                            <div class="card-body d-flex flex-column justify-content-between p-0 pr-3 pt-3 pb-3 pl-3">
                                <!-- Baris 1: NRP dan Jabatan -->
                                <div class="mb-1">
                                    <span id="cardTitle" class="card-title mb-0">NRP MR Meiji</span><br>
                                    <span class="card-subtitle text-muted">Medical Representative</span>
                                </div>

                                <!-- Baris 2: Time In dan Time Out -->
                                <div class="d-flex justify-content-between mb-1">
                                    <div class="time-box">
                                        <p>Time In</p>
                                        <span>08:00</span>
                                    </div>
                                    <div class="time-box">
                                        <p>Time Out</p>
                                        <span>16:45</span>
                                    </div>
                                </div>

                                <!-- Baris 3: Button Absen -->
                                <div>
                                    <button class="absent-button mt-2 w-100">Record Time</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-lg-4 d-flex flex-column align-items-stretch">
                    <div class="card info-card sales-card" style="height: 100%">

                        <!-- <div class="filter">
                          <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                          <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                            <li class="dropdown-header text-start">
                              <h6>Filter</h6>
                            </li>

                            <li><a class="dropdown-item" href="#">Today</a></li>
                            <li><a class="dropdown-item" href="#">This Month</a></li>
                            <li><a class="dropdown-item" href="#">This Year</a></li>
                          </ul>
                        </div> -->

                        <div class="card-body">
                            <h5 class="card-title">Visit Target <span>| This Month</span></h5>

                            <div class="d-flex align-items-center">
                                <div class="card-icon rounded-circle d-flex align-items-center justify-content-center">
                                    <i class="bi bi-bicycle"></i>
                                </div>
                                <div class="ps-3 w-100">
                                    <!-- Bagian ini akan diubah dengan visitCount / visitTarget -->
                                    <h6 class="mb-2" id="visit-count" >0 / 0</h6>

                                    <!-- Bagian ini akan diubah sesuai dengan needToVisit atau achievementRate -->
                                    <span id="visit-target" class="text-success small mt-1 mb-2 pt-3 fw-bold">0 visit</span>

                                    <!-- Progress bar akan di-update sesuai achievementRate -->
                                    <div class="progress mt-2">
                                        <div class="progress-bar" role="progressbar" style="width: 0%" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </div>
                </div>

                <div class="col-lg-4 d-flex flex-column align-items-stretch">
                    <div class="card info-card sales-card" style="height: 100%">

                        <div class="card-body">
                            <h5 class="card-title">Top Selling Product</h5>

                            <div class="d-flex flex-row justify-content-between mx-auto" style="width: 80%;">
                                <div class="card-icon rounded-circle d-flex flex-row align-items-center justify-content-center">
                                    <i class="bi bi-person-fill"></i>
                                </div>
                                <div class="card-icon rounded-circle d-flex flex-row align-items-center justify-content-center">
                                    <i class="bx bx-mail-send"></i>
                                </div>
                                <div class="card-icon rounded-circle d-flex flex-row align-items-center justify-content-center">
                                    <i class="bx bx-world"></i>
                                </div>
                            </div>
                        </div>

                    </div>
                </div>
            </div>

            <div class="row gx-2">
                <!-- Left side columns -->
                <div class="col-lg-8">
                    <div class="row">

                        <!-- Sales Card -->
                        <div class="col-xl-6 col-md-6 left-card">
                            <div class="card info-card sales-card">

                                <!-- <div class="filter">
                                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <li class="dropdown-header text-start">
                                      <h6>Filter</h6>
                                    </li>

                                    <li><a class="dropdown-item" href="#">Today</a></li>
                                    <li><a class="dropdown-item" href="#">This Month</a></li>
                                    <li><a class="dropdown-item" href="#">This Year</a></li>
                                  </ul>
                                </div> -->

                                <div class="card-body">
                                    <h5 class="card-title">Sales <span>| Yesterday</span></h5>

                                    <div class="d-flex align-items-center">
                                        <div class="card-icon rounded-circle d-flex align-items-center justify-content-center">
                                            <i class="bi bi-cart"></i>
                                        </div>
                                        <div class="ps-3">
                                            <h6>145</h6>
                                            <span class="text-success small pt-1 fw-bold">12%</span> <span class="text-muted small pt-2 ps-1">increase</span>

                                        </div>
                                    </div>
                                </div>

                            </div>
                        </div><!-- End Sales Card -->
                        <!-- Revenue Card -->
                        <div class="col-xl-6 col-md-6 right-card">
                            <div class="card info-card revenue-card">

                                <!-- <div class="filter">
                                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <li class="dropdown-header text-start">
                                      <h6>Filter</h6>
                                    </li>

                                    <li><a class="dropdown-item" href="#">Today</a></li>
                                    <li><a class="dropdown-item" href="#">This Month</a></li>
                                    <li><a class="dropdown-item" href="#">This Year</a></li>
                                  </ul>
                                </div> -->

                                <div class="card-body">
                                    <h5 class="card-title">Revenue <span>| This Month</span></h5>

                                    <div class="d-flex align-items-center">
                                        <div class="card-icon rounded-circle d-flex align-items-center justify-content-center">
                                            <i class="bi bi-currency-dollar"></i>
                                        </div>
                                        <div class="ps-3">
                                            <h6>Rp 80.000.000</h6>
                                            <span class="text-success small pt-1 fw-bold">8%</span> <span class="text-muted small pt-2 ps-1">increase</span>

                                        </div>
                                    </div>
                                </div>

                            </div>
                        </div><!-- End Revenue Card -->

                        <div class="col-xl-6 col-md-6 left-card">
                            <!-- Actual Sales -->
                            <div class="card info-card">
                                <!-- <div class="filter">
                                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <li class="dropdown-header text-start">
                                      <h6>Filter</h6>
                                    </li>

                                    <li><a class="dropdown-item" href="#">Today</a></li>
                                    <li><a class="dropdown-item" href="#">This Month</a></li>
                                    <li><a class="dropdown-item" href="#">This Year</a></li>
                                  </ul>
                                </div> -->

                                <div class="card-body pb-0">
                                    <h5 class="card-title">Actual Sales <span>| Monthly</span></h5>

                                    <div id="lineChart" ></div>

                                    <script>
                                        document.addEventListener("DOMContentLoaded", () => {
                                            new ApexCharts(document.querySelector("#lineChart"), {
                                                series: [{
                                                    name: "Desktops",
                                                    data: [10, 41, 35, 51, 49, 62, 69, 91, 148]
                                                }],
                                                chart: {
                                                    height: 250,
                                                    type: 'line',
                                                    zoom: {
                                                        enabled: false
                                                    }
                                                },
                                                dataLabels: {
                                                    enabled: false
                                                },
                                                stroke: {
                                                    curve: 'straight'
                                                },
                                                grid: {
                                                    row: {
                                                        colors: ['#f3f3f3', 'transparent'], // takes an array which will be repeated on columns
                                                        opacity: 0.5
                                                    },
                                                },
                                                xaxis: {
                                                    categories: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep'],
                                                }
                                            }).render();
                                        });
                                    </script>

                                </div>
                            </div><!-- End Actual Sales -->
                        </div>

                        <div class="col-xl-6 col-md-6 right-card">
                            <!-- Visit -->
                            <div class="card info-card">
                                <!-- <div class="filter">
                                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <li class="dropdown-header text-start">
                                      <h6>Filter</h6>
                                    </li>

                                    <li><a class="dropdown-item" href="#">Today</a></li>
                                    <li><a class="dropdown-item" href="#">This Month</a></li>
                                    <li><a class="dropdown-item" href="#">This Year</a></li>
                                  </ul>
                                </div> -->

                                <div class="card-body pb-0">
                                    <h5 class="card-title"><a asp-area="" asp-controller="Dashboard" asp-action="DashboardVisit">Actual Visit</a></h5>

                                    <!-- Column Chart -->
                                    <div id="columnChart"></div>

                                    
                                    <!-- End Column Chart -->

                                </div>
                            </div><!-- End Visit -->
                        </div>

                        <div class="col-xl-6 col-md-6 left-card">
                            <!-- Visit -->
                            <div class="card info-card">
                                <!-- <div class="filter">
                                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <li class="dropdown-header text-start">
                                      <h6>Filter</h6>
                                    </li>

                                    <li><a class="dropdown-item" href="#">Today</a></li>
                                    <li><a class="dropdown-item" href="#">This Month</a></li>
                                    <li><a class="dropdown-item" href="#">This Year</a></li>
                                  </ul>
                                </div> -->

                                <div class="card-body pb-0">
                                    <h5 class="card-title"><a asp-area="" asp-controller="Dashboard" asp-action="DashboardQuiz">Quiz E-Test</a></h5>

                                    <!-- Column Chart -->
                                    <div id="columnChartTest"></div>
                                    <!-- End Column Chart -->

                                </div>
                            </div><!-- End Visit -->
                        </div>
                         
                        <div class="col-xl-6 col-md-6 right-card">
                            <!-- Website Traffic -->
                            <div class="card info-card">
                                <!-- <div class="filter">
                                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <li class="dropdown-header text-start">
                                      <h6>Filter</h6>
                                    </li>

                                    <li><a class="dropdown-item" href="#">Today</a></li>
                                    <li><a class="dropdown-item" href="#">This Month</a></li>
                                    <li><a class="dropdown-item" href="#">This Year</a></li>
                                  </ul>
                                </div> -->

                                <div class="card-body pb-0">
                                    <h5 class="card-title">Website Traffic <span>| Today</span></h5>

                                    <div id="trafficChart" style="height: 265px;" class="echart mb-0"></div>

                                    <script>
                                        document.addEventListener("DOMContentLoaded", () => {
                                            echarts.init(document.querySelector("#trafficChart")).setOption({
                                                tooltip: {
                                                    trigger: 'item',
                                                    textStyle: {
                                                        fontSize: 9 // Ukuran font untuk tooltip
                                                    }
                                                },
                                                legend: {
                                                    top: '0%',
                                                    left: 'center',
                                                    textStyle: {
                                                        fontSize: 9 // Ukuran font untuk legend
                                                    }
                                                },
                                                series: [{
                                                    name: 'Access From',
                                                    type: 'pie',
                                                    radius: ['40%', '70%'],
                                                    avoidLabelOverlap: false,
                                                    label: {
                                                        show: false,
                                                        position: 'center',
                                                        fontSize: 9
                                                    },
                                                    emphasis: {
                                                        label: {
                                                            show: true,
                                                            fontSize: '9',
                                                            fontWeight: 'bold'
                                                        }
                                                    },
                                                    labelLine: {
                                                        show: false
                                                    },
                                                    data: [{
                                                        value: 1048,
                                                        name: 'Entertainment'
                                                    },
                                                    {
                                                        value: 735,
                                                        name: 'Registrasi'
                                                    },
                                                    {
                                                        value: 580,
                                                        name: 'Bensin'
                                                    },
                                                    {
                                                        value: 484,
                                                        name: 'Parkir'
                                                    },
                                                    {
                                                        value: 300,
                                                        name: 'Survey'
                                                    }
                                                    ]
                                                }]
                                            });
                                        });
                                    </script>

                                </div>
                            </div>
                            <!-- End Website Traffic -->

                        </div>

                    </div>
                </div><!-- End Left side columns -->
                <!-- Right side columns -->
                <div class="col-lg-4">
                    <div class="row">

                        <div class="col-sm-6 col-md-12 card-visit">
                            <!-- Recent Activity -->
                            <div class="card">
                                <!-- <div class="filter">
                                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <li class="dropdown-header text-start">
                                      <h6>Filter</h6>
                                    </li>

                                    <li><a class="dropdown-item" href="#">Today</a></li>
                                    <li><a class="dropdown-item" href="#">This Month</a></li>
                                    <li><a class="dropdown-item" href="#">This Year</a></li>
                                  </ul>
                                </div> -->

                                <div class="card-body">
                                    <h5 class="card-title">Visit Plan <span>| Today</span></h5>

                                    <div id="activity-container" class="activity"></div>
                                    <div id="empty-placeholder" class="text-center" style="display: none;">
                                        <img src="~/assets/img/planning3d.png" alt="No Visits" style="width: 160px; margin: 0px auto;">
                                        <p class="card-title">Visit Plan kamu masih kosong nih...</p>
                                    </div>
                                    <a id="show-more" class="btn btn-primary mt-2" style="display: none;">Selengkapnya</a>
                                    <a id="show-less" class="btn btn-secondary mt-2" style="display: none;">Kecilkan</a>
                                </div>
                            </div><!-- End Recent Activity -->
                        </div>

                        <div class="col-sm-6 col-md-12 card-visit">
                            <!-- Recent Activity -->
                            <div class="card info-card">

                                <div class="filter">
                                  <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                                  <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow p-2">
                                    <li class="dropdown-header text-start">
                                      <h6>Filter Date</h6>
                                    </li>
                                    <li>
                                        <div class="form-group-index p-2">
                                        <input type="date" id="startDate" class="form-control">
                                        </div>
                                    </li>
                                    <li>
                                        <div class="form-group-index p-2">
                                        <input type="date" id="endDate" class="form-control">
                                        </div>
                                    </li>
                                  </ul>
                                </div>

                                <div class="card-body">

                                    <div class="d-flex justify-content-between">
                                        <h5 class="card-title">Visit Plan <span id="later-date">| Later</span></h5>
                                        @* <div class="date-filters mt-2 mb-4">
                                            <div class="form-group p-0">
                                                <input type="date" id="startDate" class="form-control">
                                            </div>
                                            <div class="form-group p-0">
                                                <input type="date" id="endDate" class="form-control">
                                            </div>
                                        </div> *@
                                    </div>

                                    <div id="activity-container2" class="activity"></div>
                                    <div id="empty-placeholder2" class="text-center" style="display: none;">
                                        <img src="~/assets/img/planning3d.png" alt="No Visits" style="width: 160px; margin: 0px auto;">
                                        <p class="card-title">Kamu belum membuat Visit Plan untuk besok...</p>
                                    </div>
                                    <a id="show-more2" class="btn btn-primary mt-2" style="display: none;">Selengkapnya</a>
                                    <a id="show-less2" class="btn btn-secondary mt-2" style="display: none;">Kecilkan</a>
                                </div>
                            </div><!-- End Recent Activity -->
                        </div>

                        <!-- Budget Report -->
                        <!-- <div class="card"> -->
                        <!-- <div class="filter">
                          <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                          <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                            <li class="dropdown-header text-start">
                              <h6>Filter</h6>
                            </li>

                            <li><a class="dropdown-item" href="#">Today</a></li>
                            <li><a class="dropdown-item" href="#">This Month</a></li>
                            <li><a class="dropdown-item" href="#">This Year</a></li>
                          </ul>
                        </div> -->
                        <!-- <div class="card-body pb-0">
                            <h5 class="card-title">Budget Report <span>| This Month</span></h5>

                            <div id="budgetChart" style="min-height: 400px;" class="echart"></div>

                            <script>
                              document.addEventListener("DOMContentLoaded", () => {
                                var budgetChart = echarts.init(document.querySelector("#budgetChart")).setOption({
                                  legend: {
                                    data: ['Allocated Budget', 'Actual Spending']
                                  },
                                  radar: {
                                    // shape: 'circle',
                                    indicator: [{
                                        name: 'Sales',
                                        max: 6500
                                      },
                                      {
                                        name: 'Administration',
                                        max: 16000
                                      },
                                      {
                                        name: 'Information Technology',
                                        max: 30000
                                      },
                                      {
                                        name: 'Customer Support',
                                        max: 38000
                                      },
                                      {
                                        name: 'Development',
                                        max: 52000
                                      },
                                      {
                                        name: 'Marketing',
                                        max: 25000
                                      }
                                    ]
                                  },
                                  series: [{
                                    name: 'Budget vs spending',
                                    type: 'radar',
                                    data: [{
                                        value: [4200, 3000, 20000, 35000, 50000, 18000],
                                        name: 'Allocated Budget'
                                      },
                                      {
                                        value: [5000, 14000, 28000, 26000, 42000, 21000],
                                        name: 'Actual Spending'
                                      }
                                    ]
                                  }]
                                });
                              });
                            </script>

                          </div>
                        </div>End Budget Report -->
                        <!-- Website Traffic -->
                        <!-- <div class="card"> -->
                        <!-- <div class="filter">
                          <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                          <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                            <li class="dropdown-header text-start">
                              <h6>Filter</h6>
                            </li>

                            <li><a class="dropdown-item" href="#">Today</a></li>
                            <li><a class="dropdown-item" href="#">This Month</a></li>
                            <li><a class="dropdown-item" href="#">This Year</a></li>
                          </ul>
                        </div> -->
                        <!-- <div class="card-body pb-0">
                            <h5 class="card-title">Website Traffic <span>| Today</span></h5>

                            <div id="trafficChart" style="min-height: 400px;" class="echart"></div>

                            <script>
                              document.addEventListener("DOMContentLoaded", () => {
                                echarts.init(document.querySelector("#trafficChart")).setOption({
                                  tooltip: {
                                    trigger: 'item'
                                  },
                                  legend: {
                                    top: '5%',
                                    left: 'center'
                                  },
                                  series: [{
                                    name: 'Access From',
                                    type: 'pie',
                                    radius: ['40%', '70%'],
                                    avoidLabelOverlap: false,
                                    label: {
                                      show: false,
                                      position: 'center'
                                    },
                                    emphasis: {
                                      label: {
                                        show: true,
                                        fontSize: '18',
                                        fontWeight: 'bold'
                                      }
                                    },
                                    labelLine: {
                                      show: false
                                    },
                                    data: [{
                                        value: 1048,
                                        name: 'Search Engine'
                                      },
                                      {
                                        value: 735,
                                        name: 'Direct'
                                      },
                                      {
                                        value: 580,
                                        name: 'Email'
                                      },
                                      {
                                        value: 484,
                                        name: 'Union Ads'
                                      },
                                      {
                                        value: 300,
                                        name: 'Video Ads'
                                      }
                                    ]
                                  }]
                                });
                              });
                            </script>

                          </div>
                        </div>End Website Traffic -->
                        <!-- News & Updates Traffic -->
                        <!-- <div class="card">
                          <div class="filter">
                            <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                            <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                              <li class="dropdown-header text-start">
                                <h6>Filter</h6>
                              </li>

                              <li><a class="dropdown-item" href="#">Today</a></li>
                              <li><a class="dropdown-item" href="#">This Month</a></li>
                              <li><a class="dropdown-item" href="#">This Year</a></li>
                            </ul>
                          </div>

                          <div class="card-body pb-0">
                            <h5 class="card-title">News &amp; Updates <span>| Today</span></h5>

                            <div class="news">
                              <div class="post-item clearfix">
                                <img src="assets/img/news-1.jpg" alt="">
                                <h4><a href="#">Nihil blanditiis at in nihil autem</a></h4>
                                <p>Sit recusandae non aspernatur laboriosam. Quia enim eligendi sed ut harum...</p>
                              </div>

                              <div class="post-item clearfix">
                                <img src="assets/img/news-2.jpg" alt="">
                                <h4><a href="#">Quidem autem et impedit</a></h4>
                                <p>Illo nemo neque maiores vitae officiis cum eum turos elan dries werona nande...</p>
                              </div>

                              <div class="post-item clearfix">
                                <img src="assets/img/news-3.jpg" alt="">
                                <h4><a href="#">Id quia et et ut maxime similique occaecati ut</a></h4>
                                <p>Fugiat voluptas vero eaque accusantium eos. Consequuntur sed ipsam et totam...</p>
                              </div>

                              <div class="post-item clearfix">
                                <img src="assets/img/news-4.jpg" alt="">
                                <h4><a href="#">Laborum corporis quo dara net para</a></h4>
                                <p>Qui enim quia optio. Eligendi aut asperiores enim repellendusvel rerum cuder...</p>
                              </div>

                              <div class="post-item clearfix">
                                <img src="assets/img/news-5.jpg" alt="">
                                <h4><a href="#">Et dolores corrupti quae illo quod dolor</a></h4>
                                <p>Odit ut eveniet modi reiciendis. Atque cupiditate libero beatae dignissimos eius...</p>
                              </div>

                            </div>< End sidebar recent posts-->
                        <!-- </div>
                          </div>End News & Updates

                        </div>End Right side columns -->

                    </div>
                </div>
        </section>

    </main><!-- End #main -->
    <a href="#" class="back-to-top d-flex align-items-center justify-content-center"><i class="bi bi-arrow-up-short"></i></a>

    <!-- Vendor JS Files -->
    <script src="~/assets/vendor/apexcharts/apexcharts.min.js"></script>
    <script src="~/assets/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="~/assets/vendor/chart.js/chart.umd.js"></script>
    <script src="~/assets/vendor/echarts/echarts.min.js"></script>
    <script src="~/assets/vendor/quill/quill.js"></script>
    <script src="~/assets/vendor/simple-datatables/simple-datatables.js"></script>
    <script src="~/assets/vendor/tinymce/tinymce.min.js"></script>
    <script src="~/assets/vendor/php-email-form/validate.js"></script>

    <!-- JS Data Table -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.4/js/jquery.dataTables.min.js"></script>

    <!-- Template Main JS File -->
    @* <script src="~/assets/js/main.js"></script> *@
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.js"></script>
    <script src="https://cdn.datatables.net/2.1.7/js/dataTables.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>

    <script>
        $(document).ready(function () {

            const startDateInput = document.getElementById("startDate");
            const endDateInput = document.getElementById("endDate");

            const today = new Date();
            const tomorrow = new Date(today);
            tomorrow.setDate(today.getDate() + 1);

            const oneMonthLater = new Date(tomorrow);
            oneMonthLater.setMonth(tomorrow.getMonth() + 1);

            // Format the date to yyyy-mm-dd
            const formatDate = (date) => {
                const year = date.getFullYear();
                const month = String(date.getMonth() + 1).padStart(2, '0');
                const day = String(date.getDate()).padStart(2, '0');
                return `${year}-${month}-${day}`;
            };

            const formatDateForDisplay = (date) => {
                const day = String(date.getDate()).padStart(2, '0');
                const monthNames = [
                    "Januari", "Februari", "Maret", "April", "Mei", "Juni",
                    "Juli", "Agustus", "September", "Oktober", "November", "Desember"
                ];
                const month = monthNames[date.getMonth()]; // Nama bulan
                const year = date.getFullYear();
                return `${day} ${month} ${year}`; // Format untuk tampilan
            };

            const formattedTomorrow = formatDate(tomorrow);
            const formattedOneMonthLater = formatDate(oneMonthLater);

            const formattedTomorrowForDisplay = formatDateForDisplay(tomorrow);
            const formattedOneMonthLaterForDisplay = formatDateForDisplay(oneMonthLater);

            // Set default values and constraints
            startDateInput.value = formattedTomorrow;
            startDateInput.min = formattedTomorrow;
            startDateInput.max = formattedOneMonthLater

            endDateInput.value = formattedOneMonthLater;
            endDateInput.min = formattedTomorrow;
            endDateInput.max = formattedOneMonthLater;

            document.getElementById('later-date').textContent = `| ${formattedTomorrowForDisplay} - ${formattedOneMonthLaterForDisplay}`;

            document.getElementById('startDate').addEventListener('change', function () {
                // Update min pada endDate berdasarkan pilihan startDate
                const startDateValue = this.value;
                document.getElementById('endDate').setAttribute('min', startDateValue);

                // Jika endDate lebih kecil dari startDate, perbarui nilai endDate
                if (document.getElementById('endDate').value < startDateValue) {
                    document.getElementById('endDate').value = startDateValue;
                }
            });

            function updateVisitLater() 
            {

                Swal.fire({
                    title: 'Loading...',
                    text: 'Mohon tunggu data sedang diambil...',
                    allowOutsideClick: false, // Cegah user menutup alert
                    showConfirmButton: false, // Hilangkan tombol OK
                    backdrop: false, // Menonaktifkan backdrop untuk memungkinkan scrolling
                    didOpen: () => {
                        Swal.showLoading(); // Tampilkan loading animation
                    }
                });

                const startDate = $('#startDate').val();
                const endDate = $('#endDate').val();
                var savedSelection = sessionStorage.getItem('dropdownSelection');

                // Konversi string tanggal ke objek Date
                const startDateObj = new Date(startDate);
                const endDateObj = new Date(endDate);

                // Format tanggal untuk tampilan
                const formattedStartDateForDisplay = formatDateForDisplay(startDateObj);
                const formattedEndDateForDisplay = formatDateForDisplay(endDateObj);


                $.ajax({
                    url: '@Url.Action("Index", "Dashboard")',  // Ganti dengan URL dari JSON kamu
                    type: 'GET',
                    dataType: 'json',
                    data: { employeeNik: savedSelection, startDate: startDate, endDate: endDate },
                    success: function (data) {

                        var visits = data.visitLater;
                        var activityContainer = $('#activity-container2');
                        var placeholder = $('#empty-placeholder2');

                        document.getElementById('later-date').textContent = `| ${formattedStartDateForDisplay} - ${formattedEndDateForDisplay}`;

                        activityContainer.empty();
                        $('#show-more2').hide();
                        $('#show-less2').hide();

                        if (visits.length === 0) {
                            // Jika VisitLater kosong, tampilkan gambar dan pesan
                            placeholder.show();
                            $('#show-more2').hide();
                            $('#show-less2').hide();
                        } else {
                            // Jika VisitLater ada isinya, sembunyikan placeholder dan tampilkan activity items
                            placeholder.hide();
                            var visibleItems = visits.slice(0, 5);
                            visibleItems.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-info';
                                var activityItem = `
                                                <div class="activity-item d-flex">
                                                    <div class="activite-label">${visit.dateVisit}</div>
                                                    <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                                    <div class="activity-content">
                                                        <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                                        ${visit.practiceName} <br>
                                                        <a class="fw-bold text-dark">${visit.prodID}</a>
                                                    </div>
                                                </div><!-- End activity item-->
                                            `;
                                activityContainer.append(activityItem);
                            });

                            // Tampilkan tombol jika ada lebih dari 5 item
                            if (visits.length > 5) {
                                $('#show-more2').show();
                            }
                        }

                        // Tombol "Selengkapnya"
                        $('#show-more2').off('click').on('click', function () {
                            activityContainer.empty(); // Hapus isi sebelumnya
                            visits.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-info';
                                var activityItem = `
                                        <div class="activity-item d-flex">
                                            <div class="activite-label">${visit.dateVisit}</div>
                                            <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                            <div class="activity-content">
                                                <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                                ${visit.practiceName} <br>
                                                <a class="fw-bold text-dark">${visit.prodID}</a>
                                            </div>
                                        </div><!-- End activity item-->
                                        `;
                                activityContainer.append(activityItem);
                            });
                            $('#show-more2').hide(); // Sembunyikan tombol "Selengkapnya"
                            $('#show-less2').show(); // Tampilkan tombol "Kecilkan"
                        });

                        // Tombol "Kecilkan"
                        $('#show-less2').off('click').on('click', function () {
                            activityContainer.empty(); // Hapus isi sebelumnya
                            var visibleItems = visits.slice(0, 5);
                            visibleItems.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-info';
                                var activityItem = `
                                        <div class="activity-item d-flex">
                                            <div class="activite-label">${visit.dateVisit}</div>
                                            <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                            <div class="activity-content">
                                                <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                                ${visit.practiceName} <br>
                                                <a class="fw-bold text-dark">${visit.prodID}</a>
                                            </div>
                                        </div><!-- End activity item-->
                                        `;
                                activityContainer.append(activityItem);
                            });
                            $('#show-less2').hide(); // Sembunyikan tombol "Kecilkan"
                            $('#show-more2').show(); // Tampilkan tombol "Selengkapnya"
                        });

                        Swal.close();

                    },
                    error: function (error) {
                        console.log("Error fetching data: ", error);
                    }
                });

            }

            function updateChart(employeeNik) {

                Swal.fire({
                    title: 'Loading...',
                    text: 'Mohon tunggu data sedang diambil...',
                    allowOutsideClick: false, // Cegah user menutup alert
                    showConfirmButton: false, // Hilangkan tombol OK
                    backdrop: false, // Menonaktifkan backdrop untuk memungkinkan scrolling
                    didOpen: () => {
                        Swal.showLoading(); // Tampilkan loading animation
                    }
                });

                $.ajax({
                    url: '@Url.Action("Index", "Dashboard")',
                    data: { employeeNik: employeeNik },
                    success: function (data) {

                        const visitTargetData = data.visitTarget[0]; // Ambil data pertama dari array visitTarget

                        const visitTarget = visitTargetData.visitTarget;
                        const visitCount = visitTargetData.visitCount;
                        const achievementRate = visitTargetData.achievementRate;
                        const needToVisit = visitTargetData.needToVisit;

                        // Update <h6> menjadi visitCount / visitTarget
                        $('#visit-count').text(visitCount + ' / ' + visitTarget);

                        // Update text jumlah visit yang dibutuhkan
                        if (achievementRate >= 100) {
                            $('#visit-target').html('Luar Biasa!</span> <span class="pt-2 ps-1">kamu sudah mencapai target visit bulan ini</span>');
                        } else {
                            $('#visit-target').html(needToVisit + ' visit</span><span class="pt-2 ps-1">lagi untuk mencapai target!</span>');
                        }

                        // Update progress bar berdasarkan achievementRate
                        $('.progress-bar').css('width', achievementRate + '%');

                        // Jika achievementRate >= 100, tambahkan teks khusus
                        if (achievementRate >= 100) {
                            $('.progress-bar').attr('aria-valuenow', 100); // Set max ke 100
                        } else {
                            $('.progress-bar').attr('aria-valuenow', achievementRate); // Set nilai saat ini ke achievementRate
                        }

                        const testResults = data.testResults;

                        // Pastikan data.totalLulus dan data.totalTidakLulus ada
                        chart.updateSeries([{
                            name: 'Jumlah Benar',
                            data: testResults.map(test => test.correctAnswers)
                        },
                        {
                            name: 'Jumlah Salah',
                            data: testResults.map(test => test.falseAnswers)
                        }]);

                        chart.updateOptions({
                            xaxis: {
                                categories: testResults.map(test => test.name),
                            }
                        });

                        const actualVisit = data.actualVisit;

                        actualVisit.forEach(test => {
                            // Jika bulanTahun berbentuk Date, konversi ke format yang diinginkan
                            const dateObj = new Date(test.bulanTahun);
                            test.bulanTahun = dateObj.toLocaleDateString('id-ID', { year: 'numeric', month: 'long' });
                        });

                        chart1.updateSeries([{
                            name: 'Grade A',
                            data: actualVisit.map(test => test.totalVisitA)
                        }, {
                            name: 'Grade B',
                            data: actualVisit.map(test => test.totalVisitB)
                        }, {
                            name: 'Grade C',
                            data: actualVisit.map(test => test.totalVisitC)
                        }]);

                        chart1.updateOptions({
                            xaxis: {
                                type: 'category',
                                categories: actualVisit.map(test => test.tanggalVisit),
                                labels: {
                                    formatter: function (value) {
                                        return value;
                                    }
                                }
                            }
                        });

                        var visits = data.visitToday;
                        var activityContainer = $('#activity-container');
                        var placeholder = $('#empty-placeholder');

                        activityContainer.empty();

                        if (visits.length === 0) {
                            // Jika VisitToday kosong, tampilkan gambar dan pesan
                            placeholder.show();
                            $('#show-more').hide();
                            $('#show-less').hide();
                        } else {
                            // Jika VisitToday ada isinya, sembunyikan placeholder dan tampilkan activity items
                            placeholder.hide();
                            var visibleItems = visits.slice(0, 5);
                            visibleItems.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-danger';
                                var activityItem = `
                                        <div class="activity-item d-flex">
                                            <div class="activite-label">${visit.visitStatus}</div>
                                            <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                            <div class="activity-content">
                                                <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                                ${visit.practiceName} <br>
                                                <a class="fw-bold text-dark">${visit.prodID}</a>
                                            </div>
                                        </div><!-- End activity item-->
                                    `;
                                activityContainer.append(activityItem);
                            });

                            // Tampilkan tombol jika ada lebih dari 5 item
                            if (visits.length > 5) {
                                $('#show-more').show();
                            }
                        }

                        // Tombol "Selengkapnya"
                        $('#show-more').off('click').on('click', function () {
                            activityContainer.empty(); // Hapus isi sebelumnya
                            visits.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-danger';
                                var activityItem = `
                                <div class="activity-item d-flex">
                                    <div class="activite-label">${visit.visitStatus}</div>
                                    <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                    <div class="activity-content">
                                        <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                        ${visit.practiceName} <br>
                                        <a class="fw-bold text-dark">${visit.prodID}</a>
                                    </div>
                                </div><!-- End activity item-->
                                `;
                                activityContainer.append(activityItem);
                            });
                            $('#show-more').hide(); // Sembunyikan tombol "Selengkapnya"
                            $('#show-less').show(); // Tampilkan tombol "Kecilkan"
                        });

                        // Tombol "Kecilkan"
                        $('#show-less').off('click').on('click', function () {
                            activityContainer.empty(); // Hapus isi sebelumnya
                            var visibleItems = visits.slice(0, 5);
                            visibleItems.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-danger';
                                var activityItem = `
                                <div class="activity-item d-flex">
                                    <div class="activite-label">${visit.visitStatus}</div>
                                    <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                    <div class="activity-content">
                                        <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                        ${visit.practiceName} <br>
                                        <a class="fw-bold text-dark">${visit.prodID}</a>
                                    </div>
                                </div><!-- End activity item-->
                                `;
                                activityContainer.append(activityItem);
                            });
                            $('#show-less').hide(); // Sembunyikan tombol "Kecilkan"
                            $('#show-more').show(); // Tampilkan tombol "Selengkapnya"
                        });

                        var visits = data.visitLater;
                        var activityContainer = $('#activity-container2');
                        var placeholder = $('#empty-placeholder2');

                        activityContainer.empty();

                        if (visits.length === 0) {
                            // Jika VisitLater kosong, tampilkan gambar dan pesan
                            placeholder.show();
                            $('#show-more2').hide();
                            $('#show-less2').hide();
                        } else {
                            // Jika VisitLater ada isinya, sembunyikan placeholder dan tampilkan activity items
                            placeholder.hide();
                            var visibleItems = visits.slice(0, 5);
                            visibleItems.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-info';
                                var activityItem = `
                                                <div class="activity-item d-flex">
                                                    <div class="activite-label">${visit.dateVisit}</div>
                                                    <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                                    <div class="activity-content">
                                                        <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                                        ${visit.practiceName} <br>
                                                        <a class="fw-bold text-dark">${visit.prodID}</a>
                                                    </div>
                                                </div><!-- End activity item-->
                                            `;
                                activityContainer.append(activityItem);
                            });

                            // Tampilkan tombol jika ada lebih dari 5 item
                            if (visits.length > 5) {
                                $('#show-more2').show();
                            }
                        }

                        // Tombol "Selengkapnya"
                        $('#show-more2').off('click').on('click', function () {
                            activityContainer.empty(); // Hapus isi sebelumnya
                            visits.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-info';
                                var activityItem = `
                                        <div class="activity-item d-flex">
                                            <div class="activite-label">${visit.dateVisit}</div>
                                            <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                            <div class="activity-content">
                                                <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                                ${visit.practiceName} <br>
                                                <a class="fw-bold text-dark">${visit.prodID}</a>
                                            </div>
                                        </div><!-- End activity item-->
                                        `;
                                activityContainer.append(activityItem);
                            });
                            $('#show-more2').hide(); // Sembunyikan tombol "Selengkapnya"
                            $('#show-less2').show(); // Tampilkan tombol "Kecilkan"
                        });

                        // Tombol "Kecilkan"
                        $('#show-less2').off('click').on('click', function () {
                            activityContainer.empty(); // Hapus isi sebelumnya
                            var visibleItems = visits.slice(0, 5);
                            visibleItems.forEach(function (visit) {
                                var badgeClass = visit.visitStatus === 'Done' ? 'text-success' : 'text-info';
                                var activityItem = `
                                        <div class="activity-item d-flex">
                                            <div class="activite-label">${visit.dateVisit}</div>
                                            <i class="bi bi-circle-fill activity-badge ${badgeClass} align-self-start"></i>
                                            <div class="activity-content">
                                                <a href="#" class="fw-bold text-dark">${visit.doctorName}</a> <br>
                                                ${visit.practiceName} <br>
                                                <a class="fw-bold text-dark">${visit.prodID}</a>
                                            </div>
                                        </div><!-- End activity item-->
                                        `;
                                activityContainer.append(activityItem);
                            });
                            $('#show-less2').hide(); // Sembunyikan tombol "Kecilkan"
                            $('#show-more2').show(); // Tampilkan tombol "Selengkapnya"
                        });

                        Swal.close();

                    },
                    error: function () {
                        console.error("Error fetching data.");
                    }
                });
            }

            var options = {
                series: [
                    {
                        name: 'Quiz Lulus',
                        data: [0]
                    },
                    {
                        name: 'Quiz Tidak Lulus',
                        data: [0]
                    }
                ],
                chart: {
                    type: 'bar',
                    height: 250,
                    stacked: true
                },
                plotOptions: {
                    bar: {
                        horizontal: false,
                        columnWidth: '55%',
                        endingShape: 'rounded'
                    },
                },
                dataLabels: {
                    enabled: true,
                    style: {
                        fontSize: '9px' // Ukuran font untuk data labels
                    }
                },
                stroke: {
                    show: true,
                    width: 2,
                    colors: ['transparent']
                },
                xaxis: {
                    categories: ['2024'],
                    labels: {
                        style: {
                            fontSize: '9px' // Ukuran font untuk label sumbu X
                        }
                    }
                },
                yaxis: {
                    title: {
                        text: 'Jumlah Pertanyaan',
                        style: {
                            fontSize: '9px' // Ukuran font untuk judul sumbu Y
                        }
                    }
                },
                colors: ['#91cc75', '#ee6666'],
                fill: {
                    opacity: 1
                },
                tooltip: {
                    style: {
                        fontSize: '9px' // Ukuran font untuk tooltip
                    },
                    y: {
                        formatter: function (val) {
                            return val + " Quiz";
                        }
                    }
                },
                legend: {
                    labels: {
                        style: {
                            fontSize: '9px' // Ukuran font untuk legenda
                        }
                    }
                }
            };

            var options1 = {
                series: [{
                    name: 'Grade A',
                    data: []
                }, {
                    name: 'Grade B',
                    data: []
                }, {
                    name: 'Grade C',
                    data: []
                }],
                chart: {
                    type: 'bar',
                    height: 250,
                    stacked: true,
                },
                plotOptions: {
                    bar: {
                        horizontal: false,
                        columnWidth: '55%',
                        endingShape: 'rounded'
                    },
                },
                dataLabels: {
                    enabled: true,
                    style: {
                        fontSize: '9px' // Ukuran font untuk data labels
                    }
                },
                stroke: {
                    show: true,
                    width: 2,
                    colors: ['transparent']
                },
                xaxis: {
                    categories: [],
                    labels: {
                        style: {
                            fontSize: '9px' // Ukuran font untuk label sumbu X
                        }
                    }
                },
                yaxis: {
                    title: {
                        text: 'Dokter',
                        style: {
                            fontSize: '9px' // Ukuran font untuk judul sumbu Y
                        }
                    },
                    labels: {
                        style: {
                            fontSize: '9px' // Ukuran font untuk label sumbu Y
                        }
                    }
                },
                fill: {
                    opacity: 1
                },
                tooltip: {
                    style: {
                        fontSize: '9px' // Ukuran font untuk tooltip
                    },
                    y: {
                        formatter: function (val) {
                            return val + " Dokter";
                        }
                    }
                },
                legend: {
                    labels: {
                        style: {
                            fontSize: '9px' // Ukuran font untuk legenda
                        }
                    }
                }
            };

            // Create and render the chart
            var chart = new ApexCharts(document.querySelector("#columnChartTest"), options);
            chart.render();

            var chart1 = new ApexCharts(document.querySelector("#columnChart"), options1);
            chart1.render();

            $(document).on('dropdownChanged', function (event, selectedValue) {
                $('#cardTitle').text(selectedValue);
                updateChart(selectedValue);
            });

            // Load the saved dropdown selection from sessionStorage
            var savedSelection = sessionStorage.getItem('dropdownSelection')
            if (savedSelection) {
                $('#cardTitle').text(savedSelection);
                updateChart(savedSelection);
            }

            startDate.addEventListener('change', updateVisitLater);
            endDate.addEventListener('change', updateVisitLater);

        });
    </script>

</body>

</html>
